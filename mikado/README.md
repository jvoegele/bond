# The Mikado Method

I'm experimenting with using the Mikado Method for refactoring Bond internals.
The `.mmd` (Mermaid) files in this directory are the "Mikado graphs" used for
refactoring in tiny steps without ever sacrificing the integrity of the system.
GitHub renders Mermaid inline, so each graph is viewable directly from the
file's blob page.
